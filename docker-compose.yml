services:
  xiaojibike:
    build:
      context: .
      dockerfile: Dockerfile
    image: xiaojibike:local
    ports:
      - "8000:8000"
    environment:
      XIAOJIBIKE_APP_DATA_DIR: /data
    volumes:
      - xiaojibike-data:/data

volumes:
  xiaojibike-data:
